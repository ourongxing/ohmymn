{
  "name": "ohmymn",
  "author": "ourongxing",
  "version": "3.2.1",
  "license": "MIT",
  "description": "Maybe the most powerful addon of Marginnote3",
  "main": "",
  "scripts": {
    "build": "rm -rf ./dist && NODE_ENV=production tsup && cd ./dist && zip -qrm \"${npm_package_name}_v$npm_package_version.mnaddon\" *",
    "dev": "tsup",
    "prettify": "prettier --write \"./**/*.{ts, json}\""
  },
  "keywords": [
    "marginote",
    "addon",
    "typescript"
  ],
  "devDependencies": {
    "@types/bluebird": "^3.5.36",
    "@types/node": "^17.0.8",
    "esbuild": "^0.14.8",
    "esbuild-plugin-copy": "^1.1.0",
    "prettier": "^2.4.1",
    "tsup": "^5.12.0",
    "typescript": "^4.5.4",
    "unplugin-auto-import": "^0.6.1"
  }
}
