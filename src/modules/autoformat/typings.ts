export const enum StandardizeSelected {
  All,
  OnlyTitle,
  OnlyExcerptText
}
export const enum AutoStandardizePreset {
  Custom,
  RemoveAllSpace,
  HalfToFull,
  AddSpace,
  RemoveCHSpace,
  RemoveRepeatSpace
}

export enum ActionKey {
  formatSelected
}
